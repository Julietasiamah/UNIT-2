/*1. Esercizio 
Esercizi sulle classi Crea una classe User per la creazione 
di oggetti di tipo “utente”. Il suo costruttore dovrà inizializzare 
ogni oggetto utente con i seguenti attributi:- firstName- lastName- age- location 
Aggiungi alla classe User anche un metodo che restituisca il confronto con l'età di un'altra persona. 
Ad esempio, date due istanze della classe utente “x” e “y” inizializzate con le proprietà sopra descritte, 
il metodo dovrà restituire una frase simile a “x è più vecchio di y” a seconda del risultato del confronto. 
Crea degli oggetti a partire dalla classe User e verifica che la comparazione tra le età funzioni correttamente.*/

/*class User {
  constructor(_firstName, _lastName, _age, _location) {
    this.firstName = _firstName;
    this.lastName = _lastName;
    this.age = _age;
    this.location = _location;
  }
}



compareAge (OtherUser) {
    if (this.age > OtherUser) {
        return (`${this.firstName} è più vecchio di ${OtherUser.firstName}.`)
    }else if (this.age < OtherUser) {
        return (`${this.firstName} è più giovane di ${OtherUser.firstName}.`)
    }else {
        return (`${this.firstName} e ${OtherUser.firstName} hanno la stessa età.`)
    }
}*/

/*2. Esercizio Esercizi sulle classi Crea un form per la creazione di oggetti “Pet” (animali domestici). 
La classe Pet dovrà essere dotata delle seguenti proprietà:- petName- ownerName- species 
// (cane, gatto, coniglio etc.)- breed // (labrador, soriano, nano etc.) Nella classe che utilizzerai per 
// creare questi oggetti aggiungi anche un metodo che restituisca true se due animali condividono lo 
// stesso padrone. Crea, raccogliendo i dati dal form, diverse istanze della classe Pet e mostrane i 
// dati in una lista sottostante.*/
